# Estado: Stream 3-B — Ollama API

Estado: completado.

Área principal:
- `backend/ocabra/api/ollama/`

Resultado actual:
- compatibilidad `/api/*` estabilizada
- `chat`, `generate`, `embeddings`, `embed`, `pull`, `show`, `tags` y `delete`
- resolución de nombres y `model_ref` compartida con la semántica OpenAI

Pendiente real:
- ninguno crítico

Referencia viva:
- `docs/PLAN.md`
- `docs/CONTRACTS.md`
